# راهنمای کد های این پروژه

این راهنما قراره با کدهایی که برای شما سوال بوجود آورده پر بشه.

اگر شما هم سوالی راجع‌به هر قسمتی از پروژه دارین، میتونین از طریق [لینک‌های من](http://alialmasi.ir/) یا [بخش پرسش و پاسخ (نیاز به اکانت GitHub داره)](http://github.com/AliAlmasi/iranianshop/discussions/new?category=%D9%BE%D8%B1%D8%B3%D8%B4-%D9%88-%D9%BE%D8%A7%D8%B3%D8%AE) ازم بپرسین.

## دیتابیس این پروژه رو چجوری راه‌بندازیم؟ چه اطلاعاتی لازم داریم براش؟ (۱۳ مرداد ۱۴۰۳)

میتونین با خوندن کد های پروژه، بفهمین چه نوع فیلد هایی برای این پروژه لازم دارین تا ساختار جدول پایگاه‌داده پروژه‌تون آماده بشه، اما من داخل فایل `iranianshop.sql` براتون این ساختار رو از قبل آماده کردم و شما میتونین از روی همین فایل sql فقط یک پایگاه‌داده رو Import کنین.

[این پست](https://topsite98.com/post/390/sql-phpmyadmin-import-export) بهتون کمک میکنه مراحل Import کردن فایل `.sql` داخل MySQL رو انجام بدین

## این `<?=` چیه؟ چرا اینو توی کتاب ندیدیم؟ (۱۵ بهمن ۱۴۰۳)

به این میگن [PHP short echo tag](https://www.php.net/manual/en/language.basic-syntax.phptags.php#:~:text=PHP%20also%20includes%20the%20short%20echo%20tag%20%3C%3F%3D%2C%20which%20is%20shorthand%20for%20%3C%3Fphp%20echo) که درواقع یه‌جور مخفف برای اینه:
```php
<?php echo "salam" ?>
```

بجای کد بالا میتونین بنویسین:

```php
<?= "salam" ?>
```

و همون‌کار رو انجام میده. فقط حواستون باشه که داخل این تگ‌ها باید فقط از مقادیر و متغیر ها استفاده کنین. یعنی چی؟ یعنی فقط باید چیز هایی باشن که بتونین اونارو درحالت عادی هم `echo` کنین.

## این پروژه رو میتونیم برای تکلیف مدرسه استفاده کنیم؟ (۴ اردیبهشت ۱۴۰۴)

خب... میتونین ولی کد های این پروژه از کتاب خیلی خیلی جلوتره. اگر هنرآموزتون از برنامه‌نویسی وب چیزی واقعا سردربیاره، با یه نگاه میتونه بفهمه که کار خودتون نیست. 

حداقل میتونین لینک این پروژه رو بدین به هوش مصنوعی و ازش بخواین بهتون توضیح بده. _دیگه دارم راهنمایی‌تون هم میکنم_. 😂

## برای تغییر دادن کد های این پروژه به چه نرم‌افزاری نیاز داریم؟ (۱ دی ۱۴۰۴)

برای کد نویسی نرم‌افزار های مختلف بسیار موجوده. خود کتاب برای این پروژه از Adobe Dreamweaver استفاده میکنه که بنظر من انتخاب مناسبی نیست. به‌شخصه بهتون پیشنهاد میکنم از [VSCode](https://code.visualstudio.com/) یا [PhpStorm](https://soft98.ir/software/programming/3845-phpstorm.html) شرکت JetBrains استفاده کنین.
